import SwiftUI

struct DashboardView: View {
    var body: some View {
        VStack {
            Text("🖤 Project Low-Key")
                .font(.largeTitle)
                .foregroundColor(.purple)
            // Add metrics, status, and navigation
        }
        .background(Color.black.ignoresSafeArea())
    }
} 